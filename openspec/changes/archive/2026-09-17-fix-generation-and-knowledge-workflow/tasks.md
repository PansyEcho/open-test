## Implementation

- [x] 1. 修复 nullable 编译、失败恢复和原任务发布闭环。
- [x] 2. 打通用途/请求/响应契约及 MQ 消息契约，精简知识视图。
- [x] 3. 修复系统关系、外部接口目录及按需下游检索。
- [x] 4. 退役 HTTP Job，并规范化历史枚举为只读代码事实。
- [x] 5. 实现网页自动分析与同任务 native 接续，防止重复执行。

## Verification

- [x] 6. 定向测试与真实 HTTP 页面验证通过，资源版本与旧页面检查正确。
- [x] 7. 完成任务范围 OCR delegation 审查并复验接受的修复。
- [x] 8. OpenSpec 严格验证通过，停止本次服务并满足归档条件。

验收记录：原失败任务在同一 handoff 正式发布 9 个 Variant，0 个校验问题；无 QA、DATA 或清理执行。HTTP 验证自动启动一次、native 交接 200、答案与发布 200，网页回显完成且 Runner 次数仍为 1。Browser 策略阻止 Codex 深链实际打开，已验证成功回执先于跳转尝试及链接身份；不声称完成桌面跳转实测。

OCR delegation 两轮审查完成，接受并修复外部接口目录与历史扫描不一致的 Medium，无争议项。随后真实页面验收修复 MQ 注解行定位，完成主 Agent 差异检查及 58 项 Python、28 项 JavaScript 复验；5 个消费者 DTO 已解析，未闭合的外部 Wallet DTO 保留未知。测试服务 8790、8791 已停止；8788 为开发前已存在的用户服务，未操作。
