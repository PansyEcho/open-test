# Tasks

## 1. Contract

- [x] 1.1 增加Candidate、只读目录和搜索查询模型
- [x] 1.2 固定Candidate不可执行契约
- [x] 1.3 增加SystemDependencyBinding、逐源码状态和DTO结构模型
- [x] 1.4 增加结构化MQ框架规则与匹配证据模型

## 2. Projection

- [x] 2.1 从语义方法投影签名、DTO、调用方和注释
- [x] 2.2 关联入口类型、provider、配置和读写线索
- [x] 2.3 持久化直接系统依赖绑定并提供管理API
- [x] 2.4 按consumer及直接provider逐系统校验scan bundle和基线
- [x] 2.5 提供只读搜索和授权详情API
- [x] 2.6 仅扫描生产源码，以固定Git路径的精确FQN MQ规则识别文件顶层具体类直接声明handler并拒绝规则符号链接逃逸
- [x] 2.7 在对应阶段前让CapabilityDraft、Setup、Fault、Cleanup和V3执行写入口失败关闭

## 3. Verification

- [x] 3.1 增加目录投影、跨系统授权、漂移、DTO和API契约测试
- [x] 3.2 增加MQ规则唯一匹配、移除规则、歧义、顶层类型、测试/嵌套/同名/规则路径逃逸反例和真实退款Listener测试
- [x] 3.3 真实注册并扫描退款与出票系统，验证未绑定/绑定/删除绑定边界
- [x] 3.4 运行本变更相关测试和OpenSpec检查
- [x] 3.5 增加Entry唯一实现、重复Candidate身份及后续阶段失败关闭测试
- [x] 3.6 使用OCR delegation和同一只读Agent复核实现与测试来源
